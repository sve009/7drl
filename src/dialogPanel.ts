import { SightMap } from "./fov";
import { UIComponent } from "./gameObject";
import { Glyph } from "./renderer";

export class DialogPanel extends UIComponent {
    refreshVisuals(sightMap: SightMap): Glyph | null {
        return null
        
    }
    updateContent(): void {
        
    }
}