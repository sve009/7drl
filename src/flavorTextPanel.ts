// import { UIComponent } from "./gameObject";
// import { Position, TextDrawable } from "./renderer";
// import { getUIManager } from "./uiManager";

// export class FlavorTextPanel extends UIComponent {
//   positionQuery: { x: number, y: number }


//   // updateContent(): Promise<void> {
      
//   // }
  
//   refreshVisuals(): void {
//       super.refreshVisuals();
//       const txt = getUIManager().gameState.currentMap.getFlavorText(this.positionQuery.x, this.positionQuery.y);
//       this.layer.addDrawable(new TextDrawable(this.boundaries.getStartX(), this.boundaries.getStartY(), txt));
//   }

// }